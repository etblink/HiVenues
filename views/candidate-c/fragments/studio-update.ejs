<div id="draft-status" class="cc-draft-status" data-revision="<%= revision %>" hx-swap-oob="outerHTML">Draft r<%= revision %></div>
<% if (family.id === 'poster') { %>
  <%- include('../compositions/poster-canvas', { graph, media, primaryActivity, oob: true }) %>
<% } else if (family.id === 'editorial') { %>
  <%- include('../compositions/editorial-canvas', { graph, media, primaryActivity, oob: true }) %>
<% } else { %>
  <%- include('../compositions/hospitality-canvas', { graph, media, primaryActivity, oob: true }) %>
<% } %>
<%- include('inspector', { graph, family, activities, primaryActivity, media, revision, draftDigest, selectedResource, compositionRegistry, mechanicRegistry }) %>
