'use strict';

const PLATFORM_NAME = 'HiVenues';

const LEGACY_FOURTH_STREET_DEPLOYMENT = Object.freeze({
  serviceName: 'hive-bar',
  releaseRoot: '/opt/hive-bar',
});

module.exports = {
  LEGACY_FOURTH_STREET_DEPLOYMENT,
  PLATFORM_NAME,
};
