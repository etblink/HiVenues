# HV-7 Juniper Works — Pre-Acceptance Living Routing Reconciliation 0.1.0

## Status

```text
DOCUMENT = LIVING_ROUTING_RECONCILIATION
MILESTONE = HV7_ADVERSARIAL_ISOLATED_SECOND_VENUE_PILOT
SECOND_VENUE_NOMINEE = JUNIPER_WORKS_COOPERATIVE
NOMINEE_REALITY = SYNTHETIC
REQUIREMENTS = JW-R001..JW-R024__FROZEN
ARCHITECTURE_CONFRONTATION = COMPLETE__READ_ONLY
PLATFORM_GENERALITY_REPAIR_PREREGISTRATION = ACCEPTED
PLATFORM_GENERALITY_REPAIR_IMPLEMENTATION_AUTHORIZATION = ACCEPTED
PLATFORM_GENERALITY_REPAIR_IMPLEMENTATION = CANDIDATE_IMPLEMENTED__PRE_ACCEPTANCE
PR = 91__DRAFT__UNMERGED
NEXT_OPERATION = HV7_JUNIPER_WORKS_PLATFORM_GENERALITY_REPAIR__PROJECT_LEAD_ACCEPTANCE_DECISION
NEXT_SUBSTANTIVE_IMPLEMENTATION = NOT_AUTHORIZED
PRODUCTION_MUTATION = NOT_AUTHORIZED
```

## Purpose

The prior post-freeze living routing correctly opened the read-only Juniper Works architecture confrontation. That confrontation is now complete. A separately preregistered and authorized bounded platform-generality repair candidate has since been implemented on PR #91.

This document advances only the **living routing**. It does not rewrite the frozen Juniper requirements, the completed architecture confrontation, the repair preregistration, the implementation authorization, or any earlier accepted transition record.

## Current acceptance boundary

Existence of the repair candidate does not establish acceptance. Before canonical integration, the exact final PR head must satisfy all of the following:

1. deterministic qualification passes on Ubuntu and Windows;
2. the complete required pinned-Chromium chain passes, including all inherited rendered suites, HV-6 native authoring, and HV-7 Juniper public/authoring proof;
3. live-Hive smoke remains skipped unless a separately authorized live read configuration exists;
4. all four final Juniper screenshots and their manifest are preserved from that exact final head;
5. the Project Lead manually inspects those screenshots and manifest; and
6. the Project Lead adjudicates every frozen `JW-R001` through `JW-R024` without weakening the frozen requirement packet or exceeding the synthetic Tier-A evidence ceiling.

If those gates pass, the Project Lead may prepare an explicit acceptance/canonical-integration decision. This document does not itself authorize a merge.

## Evidence ceiling

Even a full synthetic Tier-A pass would establish only that the repaired Hive-Venues architecture supports the frozen Juniper Works synthetic nominee under the preregistered evidence model.

It would **not** establish:

- a second real client;
- independent real-operator usability;
- real venue permission or admission;
- real Hive onboarding;
- production deployment readiness;
- shared-runtime multi-tenancy readiness; or
- authorization for production mutation, Hive writes, payments, secrets, DNS/VPS/systemd changes, venue outreach, or deployment.

Fourth Street Bar remains the first real client, first venue nominee, sole real client, and reference deployment unless a separately authorized later operation changes those facts.

## Preserved historical records

The following remain historical/read-only and must not be silently rewritten merely to update current routing:

- `HV7_SECOND_VENUE_NOMINEE_JUNIPER_WORKS_REQUIREMENTS_0_1_0.md`;
- `HV7_JUNIPER_WORKS_POST_FREEZE_LIVING_ROUTING_RECONCILIATION_0_1_0.md`;
- `HV7_JUNIPER_WORKS_ARCHITECTURE_CONFRONTATION_0_1_0.md`;
- `HV7_JUNIPER_WORKS_PLATFORM_GENERALITY_REPAIR_PREREGISTRATION_0_1_0.md`; and
- `HV7_JUNIPER_WORKS_PLATFORM_GENERALITY_REPAIR_IMPLEMENTATION_AUTHORIZATION_0_1_0.md`.

The moving current-state surfaces are `README.md`, `docs/ROADMAP.md`, and this reconciliation.
