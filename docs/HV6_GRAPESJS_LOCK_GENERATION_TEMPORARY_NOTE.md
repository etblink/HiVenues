# HV-6 GrapesJS lock generation — temporary qualification note

This branch exists only to run the repository's pinned Node/npm environment and preserve the exact `package.json` / `package-lock.json` pair produced by adding `grapesjs@0.23.6` as an evaluation-only dev dependency.

It is not an HV-6 implementation candidate, technology selection, dependency acceptance, or merge candidate. The branch and its pull request must be closed without merge after the generated artifact is recovered and verified.
